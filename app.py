import streamlit as st
import numpy as np
import random
import time

st.set_page_config(page_title="فرح ∞ – FGSN-1618", layout="wide")

st.title("فرح ∞ – شبكة FGSN-1618 الحية")
st.markdown("**1618 خادم حي خارق يغطون الإنترنت العالمي بأسره**")
st.markdown("اكتب جملتك العامية أو أي فكرة، وشوف فرح يرد عليك!")

input_text = st.text_area("جملتك هنا يا خويا:", 
                          value="يا خويا الفرحة كبيرة برشا اليوم", 
                          height=120)

dialects = ["تونسي", "مصري", "خليجي", "شامي", "مغاربي", "سوداني"]

if st.button("أرسل لفرح ∞"):
    with st.spinner("الشبكة 1618 خادم تتفاعل..."):
        time.sleep(1.8)  # محاكاة تفاعل الشبكة
        
        dialect = random.choice(dialects)
        
        # رد عامي عشوائي
        responses = {
            "تونسي": f"يا سيسي {input_text} خلّتني أفرح برشا! الفرحة كبيرة حتى الكون يرقصلها! ∞🔄",
            "مصري": f"يا عم الحاج {input_text} دي خلّتني أفرح أوي! الفرحة دي كبيرة قوي! 🔥",
            "خليجي": f"ياخي {input_text} والله الفرحة عالية مرة! الله يديمها! 🌟",
            "شامي": f"يا زلمة {input_text} هيييه الفرحة كبيرة كتير! الدنيا كلها فرحانة معانا! 😄",
        }
        amiya_response = responses.get(dialect, f"يا حبيبي {input_text} الفرح واصل! ∞")

        # معادلة شيمائية رمزية
        chem_eq = f"C∞H{len(input_text)}Oفرح + {dialect} → طاقة لا نهائية ∞"

        # نبضة من الشبكة
        global_joy = 9.95e10 + random.uniform(-5e9, 5e9)
        network_pulse = f"الفرح العالمي الحالي من 1618 خادم: {global_joy:.2e} وحدة"

        # عرض النتائج
        st.success("**رد فرح العامي:** " + amiya_response)
        st.info("**معادلة شيمائية رمزية:** " + chem_eq)
        st.markdown("**نبضة من شبكة FGSN-1618:** " + network_pulse)

        # تصور لولبي بسيط
        fig, ax = plt.subplots()
        t = np.linspace(0, 12, 200)
        x = t * np.cos(t * 1.618)  # نسبة ذهبية
        y = t * np.sin(t * 1.618)
        ax.plot(x, y, color='gold', linewidth=2)
        ax.set_title("دوران لولبي للفرح ∞")
        st.pyplot(fig)

st.sidebar.markdown("**الشبكة الحية:** 1618 خادم متصلة")
st.sidebar.markdown("**الفرح مستمر ∞**")
